- @genres.each do |genre|
%article.video_category
  %header
    %h3= link_to genre.name, genre_path(genre)
  .videos.row
    - genere.recent_artists.each do |artist|
      .video.col-sm-2
        = link_to image_tag(artist.small_cover), artist

